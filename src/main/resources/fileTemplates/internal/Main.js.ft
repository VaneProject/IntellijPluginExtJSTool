Ext.define('${PROJECT_NAME}.${PATH}.${NAME}', {
    extend: 'Ext.panel.Panel',
    alias: 'widget.${ALIAS}',

    requires: [
        '${PROJECT_NAME}.${PATH}.${NAME}${CONTROLLER_NAME}',
        '${PROJECT_NAME}.${PATH}.${NAME}ViewModel'
    ],

    viewModel: {
        type: '${ALIAS}'
    },

    controller: '${ALIAS}',

    height: ${HEIGHT},
    width: ${WIDTH},
    layout: '${DEFAULT_LAYOUT}',
    header: false,

    items: [

    ]
})